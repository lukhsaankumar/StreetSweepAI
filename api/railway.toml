# Use the newer builder to avoid deprecation issues
[build]
builder = "RAILPACK"

[build.buildPlan.phases.install]
# 'skip = true' prevents Railway from looking for requirements.txt in the root
skip = true
cmds = ["pip install -r requirements.txt"]

[deploy]
# Ensure your start command also points to the right path
startCommand = "python main.py"